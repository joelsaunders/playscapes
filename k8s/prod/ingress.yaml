apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: ingress
  namespace: playscapes
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-staging
    kubernetes.io/ingress.class: traefik
    ingress.kubernetes.io/ssl-redirect: "true"
    ingress.kubernetes.io/ssl-host: "www.playscapespresents.com"
    ingress.kubernetes.io/ssl-force-host: "true"
spec:
  tls:
    - secretName: www-playscapes-com-tls
      hosts:
        - www.playscapespresents.com
        - playscapespresents.com
  rules:
    - host: www.playscapespresents.com
      http:
        paths:
          - path: /
            backend:
              serviceName: playscapes-nginx
              servicePort: 80
    - host: playscapespresents.com
      http:
        paths:
          - path: /
            backend:
              serviceName: playscapes-nginx
              servicePort: 80